#ifndef CSV_PARSER_H
#define CSV_PARSER_H

#include <string>
#include <vector>

namespace backtester {
namespace data {

class CsvParser {
public:
    CsvParser();
    ~CsvParser();
    // TODO: Add methods for parsing CSV data
    // For example, using rapidcsv, but we'll leave it empty for now
};

} // namespace data
} // namespace backtester

#endif // CSV_PARSER_H